# greeter

A tiny greeting utility.

## Usage

Source `src/greet.sh` and call `say_hello` with a name:

```bash
source src/greet.sh
say_hello "world"   # => Hello, world!
```

`src/main.sh` is a worked example of the same call.
